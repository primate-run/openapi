import type Recipe from "#Recipe";

const recipe: Recipe = {
  description: "Stripe",
  reference: "https://docs.stripe.com/api",
  upstream: "https://raw.githubusercontent.com/stripe/openapi/refs/heads/master/openapi/spec3.json",
};

export default recipe;
